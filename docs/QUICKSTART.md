# 🚀 QUICKSTART.md — One-Minute Setup Guide

BuildStamp lets you embed build metadata — version, commit, timestamp, and tags — into your projects automatically.

This guide covers installation, configuration, and your first successful run.

## ⚙️ 1. Install
### ✅ Option A — .NET Global Tool
```
dotnet tool install --global Solcogito.BuildStamp

```
###✅ Option B — PowerShell Installer
```
iwr https://raw.githubusercontent.com/Solcogito/BuildStamp/main/install.ps1 -useb | iex

```
## 🧠 Tip: Restart your terminal after installation so buildstamp is available globally.

###🧩 2. Create a Simple Project Structure
```
/MyProject
 ├─ src/
 │   └─ Program.cs
 ├─ Builds/
 ├─ version.json
 └─ build.ps1

```
### 🧱 3. Run Your First Stamp
```
buildstamp --out ./Builds/buildinfo.json

```
### 🧾 Output
```
{
  "project": "MyProject",
  "version": "0.7.2",
  "branch": "main",
  "commit": "f62b7e3",
  "timestamp": "2025-11-04T15:42:11Z"
}

```
### 💡 4. Embed Metadata in Your Code (C#)
```
buildstamp --format cs --out ./src/BuildInfo.cs

```
### Result:
```
// Auto-generated by BuildStamp
namespace MyProject {
    internal static class BuildInfo {
        public const string Version = "0.7.2";
        public const string Commit = "f62b7e3";
        public const string Timestamp = "2025-11-04T15:42:11Z";
    }
}

```
### 📄 5. (Optional) Add a Config File

Create buildstamp.config.json in your root directory:
```
{
  "project": "MyProject",
  "format": "cs",
  "out": "./src/BuildInfo.cs",
  "tags": [ "beta", "ci" ],
  "includeGit": true
}

```
Then simply run:
```
buildstamp

```
### 🧰 6. Combine with AutoVersion (Recommended)

For complete versioning automation:
```
autoversion bump patch
buildstamp

```
This ensures the version is updated before the metadata is stamped.

### ⚡ 7. Use in CI/CD

GitHub Actions example:
```
- name: Generate Build Metadata
  run: |
    dotnet tool install --global Solcogito.BuildStamp
    buildstamp --format json --out ./Builds/buildinfo.json

```

### 🧾 8. (Optional) Markdown for Release Notes
```
buildstamp --format md --out ./Docs/BUILD_INFO.md

```
Result:
```
**Version:** 0.7.2  
**Branch:** main  
**Commit:** f62b7e3  
**Built:** 2025-11-04 15:42 UTC

```
### 🧠 9. Verify Everything Worked
```
cat ./Builds/buildinfo.json

```
You should see all the build details for your project — including version, commit, and timestamp.

### 🧩 10. Cleanup and .gitignore

Add this to .gitignore:
```
/Builds/buildinfo.json
/src/BuildInfo.cs

```
Generated files don’t need to be committed unless required for release.

### 🧩 11. Summary
|Step|	Command	|Purpose|
|----|-----|----|
|1	|dotnet tool install --global Solcogito.BuildStamp|	Install the CLI|
|2	|buildstamp	|Generate metadata|
|3	|--format cs|	Embed in source|
|4	|--format md	|Generate Markdown summary|
|5	|--config	|Use pre-defined settings|

## 🔗 Related Docs
|File	|Description|
|----|----|
|README.md|	Overview and installation|
|BUILDSTAMP.md|	CLI reference|
|CONFIG.md|	Configuration options|
|WORKFLOWS.md|	CI/CD integration|
|TEMPLATES.md|	Output format templates|
|FAQ.md|	Common questions|

## 🪪 License

MIT License (Lite) • Commercial License (Pro)
(c) 2025 Solcogito S.E.N.C.